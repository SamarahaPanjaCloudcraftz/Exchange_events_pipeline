# Matches the cadence already documented in README.md's "Scheduling" section.
[Unit]
Description=Run exchange-events ingest every 6 hours

[Timer]
OnCalendar=*-*-* 0/6:00:00
Persistent=true

[Install]
WantedBy=timers.target
